# the-couch-FE

test

 $ yarn install
 $ cd ios && pod install 
  $ cd ..
  $ cd ..
or
  $ npm run ios
or
  $ yarn android


Must have deguuger open for app to load (http://localhost:8081/debugger-ui/)
